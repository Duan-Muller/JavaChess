package com.chess.engine;

public enum Alliance {                                                                                                  //Using enum to keep alliance typesafe
    WHITE,
    BLACK
}
